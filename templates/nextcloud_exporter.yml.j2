{{ ansible_managed | comment }}

server: "{{ nextcloud_exporter_nc_url }}"
username: "{{ nextcloud_exporter_nc_username }}"
password: "{{ nextcloud_exporter_nc_password }}"
listenAddress: "{{ nextcloud_exporter_listen_address }}"
timeout: "{{ nextcloud_exporter_timeout }}"